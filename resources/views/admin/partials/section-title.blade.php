<div class="admin-panel-title mb-8 px-6 py-5">
    <span class="font-mono text-xs uppercase tracking-[0.2em] text-neutral-400">{{ $number }} / {{ $eyebrow }}</span>
    <h2 class="mt-2 font-serif text-4xl">{{ $title }}</h2>
    <p class="mt-3 max-w-2xl text-sm leading-relaxed text-neutral-300">{{ $description }}</p>
</div>
